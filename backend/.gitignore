# Go binaries (bare names match legacy stray binaries at backend root;
# NOT used as patterns — keep specific to files, never directories)
*.exe
*.test
*.out
/server
/share-admin
/share-management-system

# Env (secrets)
.env
.env.*
!.env.example

# Logs
*.log

# Uploaded files
uploads/
