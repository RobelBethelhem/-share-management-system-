# Origin of the backend API. No trailing slash.
# Local dev: leave unset -> falls back to http://localhost:8080
# Vercel: set to your Railway backend URL, e.g. https://share-backend-production.up.railway.app
VITE_API_URL=
